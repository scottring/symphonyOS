import { BedtimePage } from '@/pages/BedtimePage'

/** Bedtime ritual surface, mounted by the Shell at /bedtime.
 * Single self-contained page (no App providers). */
export function BedtimeApp() {
  return <BedtimePage />
}
